{
  "name": "linkedin-linker",
  "version": "1.0.0",
  "description": "A Chrome extension that automates LinkedIn connection requests with sequential processing, random delays, and safety features.",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp popup.html dist/ && cp manifest.json dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "typescript": "^5.9.3"
  }
}
